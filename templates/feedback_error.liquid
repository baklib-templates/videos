<main class="py-16">
  <div class="mx-auto max-w-2xl px-4 sm:px-6">
    <div class="card p-8 text-center border-red-200">
      <div class="text-2xl font-extrabold text-slate-900">提交失败</div>
      <p class="mt-3 text-slate-600">{{ errors | default: '请稍后重试。' }}</p>
      <div class="mt-6">
          <a class="btn btn-outline" href="/s/apply" data-turbo-frame="main-content-frame" data-turbo-action="advance">返回表单</a>
      </div>
    </div>
  </div>
</main>

